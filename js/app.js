/* ===========================
   Mepiache Inventario - DEPRECADO
   ---------------------------------------------
   Este archivo correspondía al dashboard antiguo (un solo
   archivo) y ya no se usa: dashboard.html ahora redirige a
   inicio.html, y la lógica equivalente vive repartida en
   inicio.js, stock.js, conteo.js, movimiento.js, historial.js
   y metricas.js.

   Se deja vacío (en vez de eliminarse) por restricciones de
   permisos del entorno. No se carga desde ningún HTML.
   =========================== */
